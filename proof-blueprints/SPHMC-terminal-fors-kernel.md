# Actual terminal FORS kernel

SPHMC arXiv:2609.06906v1 Algorithm3.3 terminal branch and TheoremA.4(2)

Construct the actual state-varying terminal FORS Markov kernel from measurable positive variance A,center u and stored reference. Construct its reference via the genuine first terminal gradient-square d/A hit. Use a fixed standard auxiliary law and put state-dependent Gaussian scaling inside the clipped cached arc estimator. Prove actual stream reparameterization preserves acceptance and successful-batch query count and identifies the output law, handling fallback differences only almost everywhere. Derive existing two-direction RN-power/log accuracy and cached query bound from TerminalSamplerAccuracyCost with proved reference residual.

Fixed genuine C2 Hessian bounds, positive dimension, ell>=2,0<epsilon<=1/2 and explicit sufficient terminal threshold. The order ell>=2 matches A4(2); the upstream B12 proof for1<ell<2 is not used. Terminal reference initialization cost is separate from1+2exp2; no free reference assumption. Reaching the terminal-state domain, source schedules, initialization cost integration, history accuracy, stage sums, full composition and both main results remain open. Fixed auxiliary reparameterization is not an assumed equality of program laws; prove it. Never claim pointwise output equality when fallback differs on never-hit streams.

# Next route: actual state-varying terminal FORS kernel

Planning only. Independent log_depth_source_review recommends this after the actual reference count packet: it constructs the still-abstract terminal sampler rather than adding another supplied-accuracy budget wrapper. Current ReferenceCarryingCost remains the exploring packet; no new SAU or Lean edits yet.

Source consumer: SPHMC Algorithm3.3 terminal branch and TheoremA.4(2). Work on the actual terminal-state subtype with fixed genuine V, measurable A(s)>0,u(s),xref(s),fixed ell>=2 and0<epsilon<=1/2, and A(s)^-1>=64 beta(sqrt(d(ell+log(1/epsilon)))+ell+log(1/epsilon)). Construct terminal reference xp(s) from stored xref via the actual terminal d/A first gradient-square hit; do not assume its residual bound.

Use fixed auxiliary nu0=Uniform(Ioc01) product stdGaussian. With g_s=gradV(xp(s)),h_s=u(s)-A(s)g_s, construct the actual measurable proposal kernel Q_s=law(h_s+sqrt(A(s)) Z). Put auxiliary Gaussian scaling sqrt(A(s)) inside the actual cached arc estimator, clip to[-1,1], and use public PoissonRejection.poisson_rejection_output with varying Q,fixednu0/fixedB1,jointly measurable W to construct a real Markov output kernel. Pointwise Measurable(output_s) is insufficient for this claim.

Actual source program identity: at fixed s map each auxiliary (r,z) to(r,sqrt(A(s))z),extend coordinatewise through each batch and every retry,keeping proposal,Poisson count and acceptance uniform unchanged. Prove this pushes fixed auxiliary stream law to the existing statewise terminal stream law; prove acceptance and full successful-batch count preserved. Output equality needs a.e. handling: public poisson_rejection_output fallback x0 is fixed,while old terminal program falls back to statecenter. Never-hit probability is zero,so defaults may differ only on that null set. Do not claim pointwise output equality on every stream unless defaults are aligned.

Then identify each constructed kernel fiber with actual clipped law and consume TerminalSamplerAccuracyCost for the existing two RN-power/log bounds,never-hit0 and cached query bound1+2exp2. Terminal reference GD initialization cost remains separate and is not hidden in that constant. The order ell>=2 matches A4(2); the upstream B12 proof for1<ell<2 and the upstream fixed-call nonconvex extension are not used here.

Reusable public parents: TerminalReferenceGradientDescent actual terminal firsthit; current variable-parameter firsthit measurability method (preserve d/A,notinnerad); PoissonRejection.poisson_rejection_output; TerminalSamplerAccuracyCost. Mathlib map_prod_map,map_map and infinitePi pushforward interfaces. Avoid global sampler infrastructure or graph scans. Open after this packet: proof enhanced trajectory reaches terminal subtype,initialization cost,source schedules,history accuracy,stage totals and both complete main results.

Pinned API reconnaissance: Mathlib/Probability/ProductMeasure.lean:485 Measure.infinitePi_map_pi (hf : forall i, Measurable(f i)) pushes an infinite product probability through coordinatewise maps to the product of each mapped law. Apply once to auxiliary streams, then once to retry streams. ProductMeasure infinitePi uses probability fibers; fixed auxiliary nu0 and actual attempt law have these instances. No new generic infinite-product foundation needed.

PoissonRejection public attempt type and TerminalSamplerAccuracyCost private attempt type are both X times(Nat times((Nat->A) times Real)). Both use q product Poisson(2B) product(auxstream product uniformIcc01). accepted event and queryCount/totalCost formulas align by definitions: accept uniform <= product Fin(n) of(B+W)/(2B), cost is tsum over retry n of batch count if every earlier retry failed. Both firstOutput/output use dependent Nat.find and fallback. Poisson public theorem has fixed x0; retain explicit a.e. fallback handling. Current private cachedEstimator is inner(pi/2*(cos(pi/2*t)*(x-h)-sin(pi/2*t)*z),g-gradV(h+sin(pi/2*t)*(x-h)+cos(pi/2*t)*z)). Auxiliary t from Ioc01 is different from acceptance uniform Icc01.
