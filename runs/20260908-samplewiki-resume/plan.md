# SampleWiki Route resumption

## Baseline and authority

- Human handoff: task `加上Discrete Sampling和MCMC`, final response dated 2026-09-07.
- Local baseline: clean `main`, `5d1211ccf013389832f6614b93373474f79ae278`.
- Safe fetch / fast-forward: `3ce020d0098a891ce86646ae85e745aaa49ae825`.
- Work branch: `codex/samplewiki-shared-stabilization`.
- Registry assertion on current main: 393 (awaiting fresh local full build).
- Pinned Lean: v4.33.0; Mathlib: db584cd6d46c92f209a44c0f1c829460d327499d.
- Goal activated in the existing human thread. No detached ASTIS session.

## Goal and completion boundary

Complete the existing source-backed SampleWiki Route and its necessary shared
foundations using dependency-ready substantive advances. The reference execution
map is `proof-blueprints/samplewiki-frontier-formalization-plan.md`, interpreted
against current Frontier Cells and source inventories. Literature-open rows stay
explicitly open; documentation of an unknown bound is not a proof obligation to
invent a new theorem. A change in research scope requires a distinct decision.

Close shared prerequisites once under their canonical owners. Their completion
does not count as a new SampleWiki paper theorem. Seven-library membership,
conceptual correspondence and actual Lean dependencies have different evidence.

Each SAU must return a compiled theorem edge, reusable interface, integration
node, or strict typed obstruction with a smaller residual. An individual SAU or
stage never completes the overall Goal. Progress reporting counts closed edges,
real consumers and remaining source boundaries, not wrapper or file counts.

## Immediate ordered queue

1. Restore local Windows execution of the current pinned Lean/Harness.
2. Recover and stabilize existing PR #246, `ASTIS-SHARED-kernel-invariant-powers`.
3. Recover and stabilize existing PR #247, `ASTIS-SHARED-conditional-resampling-law`.
4. Construct one-block heat-bath kernel using the conditional-distribution API.
5. Prove preservation of the original joint law, retaining representative/support conditions.
6. Reuse or prove invariant random-scan kernel mixtures; reuse finite powers.
7. Select the next dependency-ready shared/SampleWiki cell from current data.

The high-reuse SampleWiki spine then joins the existing probability, coupling,
Wasserstein, Fisher/entropy and heat/backward-flow dependencies toward ideal
proximal sampling and its paper-level implementations. LMC/ULD/MALA, stochastic
oracles, mirror geometry and convex-body/lower-bound branches retain their exact
source regimes and dependency partial order. General-state MH flux/support and
finite-jump MLSI zero-density domains remain separately audited branches.

## Execution and cost discipline

One Worker owns each mathematical packet; an independent reviewer checks it.
Use compact cell synthesis, exact interfaces and targeted searches. Keep shared
imports, Registry and graphs under one stabilization owner. Prefer focused Lean
checks before root builds; run root/site gates once after a coherent integration.
After the first occurrence and two unchanged repeated failures, diagnose the
statement/route instead of another tactic retry. No unrequested numeric token
budget or completion estimate is imposed.

## Verified incoming failures

- PR #246, head 9bb0a581e58d233bcd5712334433ab756bd77e65,
  Actions job 101812902813: `Tests/KernelInvariance.lean:18` fails to synthesize
  `HPow (Kernel α α) ℕ (α → Measure α)`; subsequent heartbeat errors are secondary.
  This is a focused Lean test failure, not merely a consistency check failure.
- PR #247, head c0500146a342e176288ba6e91ca4611c6e8cfbee,
  Actions job 101815164267: invalid `mcmc_contract.invariance_class` blocks tests
  before Lean. Pinned Mathlib additionally accepts only the `Y` measurability
  argument in `compProd_map_condDistrib`, unlike the incoming proof call.
- Native Windows Harness imports `fcntl` unconditionally; atomic publication
  also uses Unix directory fsync. These are execution prerequisites, not math.

Both Frontier Cells remain `claimed` until actual focused Lean evidence exists.
The source modules/tests were recovered from those exact PR heads; no upstream
PR status, proof status, Registry count or public graph has been promoted.

## Local execution recovery

- Exact Lean 4.33.0 is installed; inherited `ELAN_TOOLCHAIN=4.29.1` must be
  overridden per invocation. No toolchain or Mathlib pin was changed.
- Local Python 3.11 cannot parse the existing site scripts (CI uses 3.12).
  The already installed Python 3.14.4 runs their 38 focused tests successfully;
  no unrelated site compatibility rewrite was made.
- Native Windows locking/publication and optional-source ATLAS validation were
  repaired. Harness/advance/ATLAS tests: 56 passed under Python 3.14.4.
- Git's Windows CRLF conversion changed 26 byte-pinned ATLAS fragments. Their
  LF-normalized bytes match every committed digest; `.gitattributes` now pins
  LF. No certification hashes or external-source claims were weakened.
- The generated Lake cache was relocated to a local disk with adequate space;
  the original repository path resolves through a local junction. Remaining
  original cache metadata is retained in the ignored relocation remainder.
  Neither absolute cache paths nor compiled artifacts enter repository truth.
