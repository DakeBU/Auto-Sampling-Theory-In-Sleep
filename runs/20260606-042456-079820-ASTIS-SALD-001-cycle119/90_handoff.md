# Handoff

Task id: `ASTIS-SALD-001`
Cycle: `119`

## Upper Decision
narrows-source-cited-boundary.

Global phase judgment: cycle 118 did not fail and needs no recovery. Phase 1
theorem-skeleton translation is stable enough for cited-theory backfill. The
single lower packet that now reduces the largest proof risk is an illness-area
refiner for the active EM conditional-drift/weak-FP sub-DAG: consume the
compiled canonical `barB` state-event witness
`SALD.generalMovingTargetDiscreteCanonicalBarBEmStateEventInterface` inside
the weak-FP generator/source-sign path for
`sald.general_moving_target_discrete.em_interpolation_fp` over
`appendix.tex:1358-1387`.

Exact boundary to narrow: after cycle 118, do not restate
`hbarBCondExp` or ask for another selected representative. Lower should target
`ASTIS.SALD.cycle110.remaining_parametric_generator_boundary_after_dominated_transport`
with `barB` fixed to the canonical `condDistrib` guide-plus-score field from
`appendix.tex:1368-1377`. The expected lower-ready theorem should either
compile a source-cited consumer that removes the remaining supplied
canonical-`barB` integrability/state-event input from the weak-FP source-sign
route, or report the smaller named theorem boundary consisting of the EM
pointwise path derivative, local domination, drift action
`driftAction = weakGradPairing canonicalBarB`, no-boundary weak divergence,
and diffusion source-action identities.

Mode discipline: faithfulPaper only. Keep the source theorem fixed, keep the
active target on `sald.general_moving_target_discrete.em_interpolation_fp`,
and synchronize only `appendix.tex:1368-1387` with the Lean and
proof-obligation views. Defer Phase 2 API cleanup and project-article export.

Non-goals: no `hbarBCondExp` wrapper churn, no broad theorem-route audit, no
source-index rebaseline, no non-EM fallback, no KL/LSI/DV/Gronwall scalar
detour, no Lake or SLT dependency change, no theorem-status promotion, no fake
closure, and no `sald_version_2.tex`.

Local references checked: the configured SLT repo and paper paths are missing,
so no SLT theorem was consulted, imported, or marked formalized. Local
Mathlib-facing files located for the relevant names were
`.lake/packages/mathlib/Mathlib/Probability/Kernel/CondDistrib.lean` and
`.lake/packages/mathlib/Mathlib/Probability/Kernel/Condexp.lean`; the upper
packet relies only on existing compiled local declarations, including
`ProbabilityTheory.condExp_prod_ae_eq_integral_condDistrib'`,
`ProbabilityTheory.condDistrib_apply_ae_eq_condExpKernel_map`,
`SALD.generalMovingTargetDiscreteCanonicalBarBEmStateEventInterface`, and
`SALD.generalMovingTargetDiscreteWeakConditionalFpNamedLawDerivativeOfDominatedSplitGeneratorHandoff`.

## Middle Formalization State
Middle should keep the conversion-window DAG table focused on:
`appendix.tex:1368-1377` canonical conditional drift, cycle-118 direct
canonical state-event witness, and `appendix.tex:1379-1387` weak-FP
generator/source-sign consumer. If the dominated-generator/source-action
composition is too large for one lower step, middle must name exactly one
smaller source-cited theorem with imports and hypotheses.

## Lower Attempts
Queued lower packet: `cycle119.canonical_barB_weak_fp_consumer_boundary`.
Start from `SALD.generalMovingTargetDiscreteCanonicalBarBEmStateEventInterface`
and the existing generator/barB-action handoffs
`SALD.generalMovingTargetDiscreteWeakConditionalFpSourceSignsOfSampleSplitGeneratorBarBActionHandoff`
and
`SALD.generalMovingTargetDiscreteWeakConditionalFpNamedLawDerivativeOfDominatedSplitGeneratorHandoff`.
Do not add a wrapper that merely republishes these declarations. Progress is
either a compiled theorem discharging a supplied canonical-`barB`
weak-FP input, or a narrower named theorem boundary for the concrete EM
pointwise derivative/domination plus drift/diffusion source actions.

## Reviewer Findings
Reviewer checklist: verify the packet classification is
`narrows-source-cited-boundary`, the target still references
`sald.general_moving_target_discrete.em_interpolation_fp` over
`appendix.tex:1358-1387`, the drift source remains the original
`appendix.tex:1368-1377`, and `python3 tools/astis.py check` passes. Reject
new supplied-hypothesis wrappers, broad route/source-index audits, non-EM
fallbacks, SLT imports, Lake changes, theorem-status promotions, fake
closures, or any `sald_version_2.tex` use.

## Next Cycle Objective
Lower should either compile the canonical-`barB` weak-FP consumer handoff or
return the exact smaller source-cited boundary under
`ASTIS.SALD.cycle110.remaining_parametric_generator_boundary_after_dominated_transport`.
