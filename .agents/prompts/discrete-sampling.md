# Discrete Sampling universal worker

Read `AGENTS.md`, `.agents/skills/astis-substantive-advance/SKILL.md`, `.agents/skills/astis-cross-library-coordination/SKILL.md`, `Libraries/DiscreteSampling/source-map.json`, `Libraries/DiscreteSampling/route-plan.json`, and `Libraries/frontloaded-shared-spine.json`. Use route `discrete-sampling`, mode `faithfulPaper` for exact source statements; explicit new generalizations use `exploratoryProof` and do not replace the original theorem.

The controlling book is CSV arXiv:2307.13826v4, not v1. Its §12 Ising/colorings statements are surveys without proofs. Retrieve and pin the cited original theorem and all missing proof details before targeting a §12 mixing result. A zero-field Ising law can be pulled forward immediately; field/boundary extensions and ferro/antiferromagnetic regimes must not be silently exchanged.

Select one SAU from the shared DAG. Search Samplinglib, pinned Mathlib, active shared cells and compatible upstreams first. Prefer existing state-neutral kernels and `FunctionalInequalities.Generator` over a second finite-state library. Match exact types, measures, admissible domains and constant conventions. Keep finite sums ↔ integrals, matrices ↔ kernels, and support restrictions as explicit adapters. A gap needed by multiple sources is one `route: shared` cell, not a private discrete copy.

Persist a schema-v2 Frontier Cell, following `_discrete_example.json` with **real**, non-placeholder fields. Distinguish finite state space from the clock: one-site P, a sweep, L=P-I and n(P-I) have different rates. Record all feasible pinnings, zero marginal conventions, irreducibility/laziness, model/graph/temperature/field region, metric, initialization and total cost. Do not infer discrete-time convergence from a positive one-sided spectral gap alone.

Mandatory conceptual audit: read `Libraries/conceptual-mirror-protocol.json`, family memory and bridge records first. Publish a typed `conceptual-mirror` discovery when a proof shares a mechanism. Preserve state/metric/energy/clock, hypotheses, conclusions, failure boundaries, sources and stable IDs. Check existing discrete PI/χ², MLSI/entropy, Maas geometry, covariance/influence and coupling families before making another. Creator and independent validator must differ; candidate previews are not accepted mirrors, and accepted mirrors are not Lean transport certificates.

Prove the exact missing adapter or a useful smaller child; do not prove the desired conclusion by assuming a newly named equivalent hypothesis. Run focused Lean tests early, `python3 tools/astis_frontier_cells.py check`, the source/semantic gates and finally `python3 tools/astis.py check`. Stabilization alone integrates aggregators, source maps and graph memory. No graph proximity, compiled substrate, imported prerequisite, metadata check or self-review upgrades a source theorem or chapter.
