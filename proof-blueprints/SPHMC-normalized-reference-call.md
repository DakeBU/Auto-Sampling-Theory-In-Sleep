# Actual normalized reference call

SPHMC arXiv:2609.06906v1 Section6.1 normalization convention and Section6.3 next smoothed-sampler call

For the actual regularized potential F_p and actual first ad-hit reference constructed by JointReferenceGradientDescent, set a=alpha+1/A,c=beta+1/A,k=c/a,S(x)=sqrt(c)*x,Fbar_p(x)=F_p(S^-1(x)). Prove actual C2 Hessian bounds[a/c,1], gradient scaling, pairwise measurable scaled reference and its squared-gradient bound d/k. Prove actual normalized Gibbs law S#pi_Fp=pi_Fbarp, with positive finite normalizers/Jacobian justified. For next smoothing variance t>=0 prove S^-1#gaussianSmoothing(pi_Fbarp,sqrt(t))=gaussianSmoothing(pi_Fp,sqrt(t/c)). For every actual normalized probability output law with squared W2 budget R^2,R>=0, derive returned inverse-scaled output squared budget R^2/c; source A1 substitutes R^2=k*epsilon^2. Do not assume these measure equalities or a reference admissibility conclusion.

Fixed-call coordinate normalization and actual-law accuracy adapter, including eta0 and next t0,positive dimension and tau>0. Consume the actual first-hit reference rather than a supplied arbitrary point. General beta and coordinate-free finite dimension are explicit extensions. The normalized sampler precision is supplied,not proved. No all-parameter target kernel,measurable sampler selection,conditional random histories,total query costs or complete-paper claim. Current eta,tau and next t are distinct.

# Next source-backed route: coordinate normalization for the actual next call

Planning only; no new SAU, Lean or completion. Independent log_depth_source_review checked source Section6.1/6.3 and accepted this direction. Source uses F(x/sqrt(c)) and returns the normalized sample divided by sqrt(c). A Hessian/reference-only adapter would not suffice: include actual Gibbs/smoothing measure equality and returned-output precision.

For the same current pair p, write F_p for its regularized potential, a and c for its true Hessian lower/upper constants, k=c/a, S(x)=sqrt(c)*x, and Fbar_p(x)=F_p(S^{-1}x). Use the actual JointReferenceGradientDescent output, not a supplied reference point. Prove genuine Hessian bounds [a/c,1], gradient Fbar_p(Sx)=c^{-1/2}gradient F_p(x), and norm(gradient Fbar_p(S*out(p)))^2<=d/k. The pair-to-scaled-reference map is measurable.

Prove actual normalized Gibbs equality S#pi_F=pi_Fbar. The Jacobian and positive finite normalization constants must cancel by proof; do not infer law identity from potential syntax. RGOCalculus proves normalized Gibbs integrability/curvature but not this coordinate-change law. Mathlib Measure.integral_comp_smul in MeasureTheory/Measure/Haar/NormedSpace.lean supplies the volume integral substitution.

For the NEXT normalized smoothing variance t>=0 (distinct from current eta,tau), prove S^{-1}#gaussianSmoothing(pi_Fbar,sqrt(t))=gaussianSmoothing(pi_F,sqrt(t/c)). Use actual product-map/map-composition identities. For any actual normalized output law nubar with W2 squared budget R^2 to that normalized smoothed target, show the returned inverse-scaled law has budget R^2/c. Source A1 specializes R^2=k*epsilon^2. Transport cost upper bound for actual pushed-forward couplings, strict near-optimal selection, and WassersteinSpace.wassersteinDistance_sq suffice; full W2 metric/isometry API is unnecessary.

Potential proof order: true differential scaling; actual stopping-point admissibility; normalized Gibbs Jacobian; actual Gaussian product law; coupling precision rescaling. Avoid scalar-only wrapper churn. StoppedRGODepth uses baseline smoothness1 and cannot replace the measure scaling proof; it need not be a parent.

This remains fixed-call mathematics. The normalized sampler guarantee is supplied; no measurable selection of a sampler over random histories, all-parameter target kernel, conditional history propagation or total cost is inferred from pointwise Gibbs identities. No new parallel Goal or detached process.

Pinned API audit: Measure.integral_comp_smul is at Mathlib/MeasureTheory/Measure/Haar/NormedSpace.lean:92 and gives integral f(R*x)=abs((R^finrank)^(-1))*integral f, with total Bochner conventions. integrable_comp_smul_iff (same file:174) and Integrable.comp_smul (:190) preserve actual L1 for nonzero scaling. Measure.tilted_apply_eq_ofReal_integral' (Measure/Tilted.lean:109) identifies each measurable set mass with ofReal of the integral of the normalized exponential density. This permits a direct setwise proof using indicator integrals and scalar substitution: normalize both numerator and denominator with the same positive Jacobian, then cancel. No general map/tilted commutation theorem was found in the narrowly searched Tilted/WithDensity files; do not spend time inventing a full density transport API if the setwise argument closes the actual consumer. Establish exponential L1 and positive normalizer from the actual strongly convex parent before algebraic cancellation.

Differential reuse: QuadraticRegularization.lean already proves the true quadratic Hessian shift internally (hWfd,hWdd,hbounds, lines45-78), although its public theorem returns only strong convexity and gradient Lipschitz continuity. Reuse this small implementation pattern privately in the actual normalization consumer if needed; do not change the parent public contract merely to expose a wrapper. Mathlib Analysis/InnerProductSpace/Calculus.lean:205-208 gives fderiv_norm_sq_apply and fderiv_norm_sq. The existing shift proof differentiates the continuous linear map J(v)=innerSL Real v, so no invented Hessian field or smoothness-to-Hessian shortcut is needed.
