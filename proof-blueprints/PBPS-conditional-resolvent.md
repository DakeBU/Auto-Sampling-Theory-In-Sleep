# Actual conditional epsilon weak resolvent

For any closed partial real-linear A between Hilbert spaces, epsilon>0 and f in H, construct the unique u in A.domain satisfying epsilon inner(u,v)+inner(Au,Av)=inner(f,v) for every domain v, with exact energy identity and norm<=epsilon^-1 norm(f), energy<=epsilon^-1 norm(f)^2. Instantiate A as the closure of the SAME actual conditional gradient D_y, selected before epsilon and f, retaining the common R,S, disintegration, reflection, potential, normalization and exact original compact gradient graph.

Abstract weak-form prerequisite and actual full-space conditional-law instance only. No pointwise PDE regularity, D*D operator core, compact Bochner extension, epsilon-uniform bound, Poincare, measurable y-to-solution selection, noncompact score variance, process or full-paper claim.

Use G_epsilon=A.graph.comap(T) where T(a,b)=(a/sqrt(epsilon),b), a continuous linear map on WithLp 2 (H x K). Closedness is a preimage of A.IsClosed. Project (f/sqrt(epsilon),0) onto G_epsilon. Test orthogonality against (sqrt(epsilon)*v,Av); recover u from the first coordinate. Test v=u for exact energy; Cauchy-Schwarz gives both bounds. Test differences for uniqueness. Abstract existence does not require a dense domain.

Retain same R,S and same D_y before all epsilon and f. No measurable parameter-family claim. The smooth core of the gradient is not automatically a core of D*D, and compact-test Bochner cannot yet be applied to the weak solution.

Independent planning: depth_commit_verifier exact API route and log_depth_source_review noncircular/source boundary accepted as planning only. Root primary-source refresh of PBPS C.1 and Kolesnikov-Milman sections2.4-2.5, plus fixed Mathlib APIs. No Lean result yet; Gradient PR257 remains sole stabilization owner.

Current checkpoint: production and actual consumer focused PASS3298, independently replayed with the standard three axioms. Independent complete proof review and fresh anonymous reconstruction plus both seven-slot source reviews accepted. Source verdict is domain-mismatch: this is an explicit abstract/full-space weak prerequisite, not the background compact-manifold Poisson regularity or source conditional Poincare theorem. Fixed-commit admission and integration remain pending. Gradient PR257 merged at dc511b5603287a597379f96e7283a5cda19570e1; deployment is checked separately.

Bounded next-route review (depth_commit_verifier, read-only): no ready local H2 elliptic regularity or weighted generator core found in fixed Mathlib. Next dependency-ready bridge should explicitly identify the closed gradient by weighted C1 compact-test integration by parts, including integrability and the exact directional test derivative. With phi=exp(W) psi, local weight/volume comparison then identifies a genuine distributional gradient. Resolvent can supply the local distributional equation, but local H2 regularity and cutoff/mollifier convergence of both u and Lu are still needed before compact Bochner extends. Lp smooth density and a gradient graph core do not give a D*D operator core. This is planning only, not a new Lean result or another Goal.
