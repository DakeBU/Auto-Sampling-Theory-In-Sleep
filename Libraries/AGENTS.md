# Libraries agent note

When working on acceleration, momentum, mirror/dual geometry, underdamped Langevin, or differing-norm routes, read these before editing route metadata:

1. `acceleration-geometry-program.json`
2. `../docs/acceleration-geometry-handoff.md`
3. `formalization-routes.yml`
4. `conceptual-mirror-protocol.json`

The Optimisation textbook is the producer for the common acceleration proof architecture. Riemannian Optimisation, Statistical Optimal Transport, and Sampling should consume shared Lean certificate algebra through explicit adapters rather than creating route-local copies.

Keep the truth classes distinct: `formal-lean`, `lean-prototype`, `source-theorem`, `proof-pattern-transfer`, and `research-hypothesis`. In particular, the Katyusha-to-sampling and general differing-norm-to-sampling transports are research routes, not theorem edges.

Do not edit append-only SAU/discovery ledgers or another active Frontier Cell merely to advertise this route. Shared aggregator/Registry/Functor-Hypergraph stabilization remains serialized.
