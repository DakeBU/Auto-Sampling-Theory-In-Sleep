# Actual reference-carrying recursive kernel

SPHMC arXiv:2609.06906v1 Algorithm3.3 and Section6.3: actual next reference in the recursive state transition

Construct the actual reference-carrying state transition for s=(b,u,xref,count,history),base C2 potential V with genuine positive Hessian bounds,measurable positive normalized eta/tau,and actual Markov current output M already in original coordinates. For x~M(s),independent standardGaussian z,set y=x+sqrt(tau/(beta0+b))*z,v=(eta+tau)/(beta0+b),bplus=b+1/v,uplus=(bplus)^-1*(b*u+v^-1*y). Starting from pre-noise x,run actual gradient descent for V+bplus/2*norm(.-uplus)^2,step1/(beta0+bplus),first gradient-square<=(alpha0+bplus)*d. Prove joint measurability,finite first-hit,normalized reference admissibility,and genuine gradient/trajectory agreement with Vs+norm(.-y)^2/(2v). Push the actual product kernel through the enhanced update and prove Markovness,explicit one-step projected RGO update and terminal self-loop. No supplied reference or sampler-precision premise.

Original-coordinate current output M is a supplied actual Markov kernel,not a supplied accuracy guarantee. Pointwise stopping and reference construction need no W2 input. General beta,coordinate-free positive dimension and arbitrary reference/count/history states are explicit; no history validity invariant or cost claimed. Only one-step projection equality; dependence of M on reference forbids automatic projected-chain Markovness. No constructed accurate smoothed sampler,state-varying terminalFORS,conditional moment,stage cost or complete main result.

# Next source-backed edge: actual reference-carrying recursive state kernel

Planning only. Independent log_depth_source_review checked SPHMC Algorithm3.3 and Section6.3 and accepted this next bounded source/dependency route. No new SAU or Lean edits yet. Current NormalizedReferenceCall remains the sole stabilization owner.

Current FiniteRGOProgram state stores precision,center,count,padded history but not the actual reference consumed by the next call. Construct one enhanced transition carrying that reference. Fixed genuine C2 V has Hessian bounds alpha0,beta0 with positive alpha0<=beta0 and positive dimension. State s=(b,u,xref,k,h),b>=0, represents V_s(w)=V(w)+b/2*norm(w-u)^2. Current measurable normalized variances eta(s)>0,tau(s)>0 and beta_s=beta0+b yield v=(eta+tau)/beta_s>0.

M(s) is an actual Markov output kernel already returned to ORIGINAL coordinates (after any normalized sampler inverse scaling), not a normalized-coordinate law. No W2 guarantee is needed to construct the actual reference program. Draw x~M(s) and independent z~stdGaussian. Set y=x+sqrt(tau/beta_s)*z, bplus=b+1/v and uplus=(bplus)^-1*(b*u+v^-1*y). Increment count and prepend y to history. The actual next GD starts at x BEFORE the new noise, never at y. Its genuine gradient is gradV(w)+bplus*(w-uplus), step1/(beta0+bplus), actual first threshold (alpha0+bplus)*d.

Shortest mathematical route: TerminalReferenceGradientDescent applied pointwise to baseV,A=1/bplus,centeruplus supplies an actual finite d*bplus witness and genuine gradient identity. Since d*bplus<=(alpha0+bplus)*d, take the actual first inner hit, without identifying it with the terminal output. This avoids unnecessarily supplying outer W2 merely to use JointReferenceGradientDescent. Prove measurable finite iterations jointly in state,x,z from explicit gradient and varying measurable bplus,uplus,step. Apply measurable_find and Measurable.find after pointwise finite existence. Scale actual new reference by sqrt(beta0+bplus), giving normalized gradient-square <=d/((beta0+bplus)/(alpha0+bplus)). Reuse current differential scaling pattern or genuine gradient calculus as needed; no duplicate contraction proof.

An actual product kernel M(s).prod(stdGaussian) and measurable enhanced update map yield a Markov transition; use a measurable terminal set and dirac-self transition for absorbed states. State-reference projection must have the explicit one-step RGO precision/center/count/history marginal. If M depends on reference, do NOT infer the projected whole process is the old Markov chain without a factorization condition. Keep full state/history. Kernel accuracy,conditional moments,cumulative costs and state-varying terminalFORS remain separate obligations.

Source-fidelity addition required by independent reviewer: explicitly prove that V_s(w)+norm(w-y)^2/(2v) and V(w)+bplus/2*norm(w-uplus)^2 differ only by a state/observation constant, hence their genuine gradients and actual GD trajectories coincide. Parameter names alone do not establish source algorithm identity.

Reusable exact public parents: TerminalReferenceGradientDescent.terminal_reference_gradient_descent (pointwise gradient/finite terminal witness), StateDependentRGO.state_dependent_recovery (actual statewise target/observation/posterior kernels), FiniteRGOProgram.finite_rgo_program (history update and absorption), RGOCalculus.rgo_calculus/RGOClosure (actual quadratic update semantics). Mathlib measurable_find,Measurable.find,Kernel.map/product/deterministic/comap/piecewise. Only narrow source/module/API reads so far; no whole graph/library scan.
