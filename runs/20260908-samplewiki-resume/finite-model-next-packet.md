# Next source/model route after positive-fiber normalization

Historical handoff before the random-scan packet. The complete page-4 support
convention and independent one-index proposal review are now recorded in
`source-support-and-repair-audit.md`; the actual transition-law implementation
is frozen at `90581ec`. Continue from `random-scan-next-packet.md`, not by
repeating the earlier limited-context audit or creating another scan kernel.

This is a dependency-ready handoff, not a frozen new theorem or an accepted
source repair. Continue the existing overall Goal and SAU queue.

## Reuse the completed mathematics

The canonical coordinate kernel already has Markovness, joint-target
invariance, retained-coordinate equality and its positive-fiber normalized
restriction law. Fixed finite mixtures and powers already preserve the target.
Do not re-prove these facts or add public aliases merely to name a Gibbs scan.
The current tests exhibit real nontrivial conditional transition probabilities
and a nonzero-target null-fiber counterexample.

## Source obligations before algorithm assimilation

The independent roundtrip audit `ASTIS-RT-20260908-PositiveFiberUpdate` diagnoses
two blocking differences from the supplied general update instructions:

1. The literal nonselected-coordinate assignment differs from retained-fiber
   conditioning. Preserve the original excerpt and its byte-pinned PDF. Any
   minimal index correction needs a separate exact-proposal review under the
   semantic-roundtrip protocol; the accepted diagnosis is not repair approval.
2. Membership in a named state set does not by itself supply positive target
   mass. Inspect the pinned source's target/support convention before choosing
   the finite specialization. For a positive-mass starting singleton, native
   measure monotonicity already supplies a positive retained fiber. Do not
   assume positivity for all ambient configurations or use a null conditional
   version as a feasible-state guarantee.

The generic theorem also has a broader domain and weaker normalization than
the finite Boolean probability source. Document the actual embedding or
restriction of the source target and the specialization of typeclasses.

## Candidate next mathematical consumer, only after this source audit

One useful integration packet is the **actual random-scan finite transition
law**, obtained from the existing fixed mixture of canonical coordinate
kernels. Its statement should expose the site's normalized weights, the
conditional denominator, unchanged-coordinate condition, target support and
one-update clock. It must supply an operational formula or supported-state
property genuinely used by the source algorithm, not only another invariant
mixture wrapper. Freeze at most one such edge after the exact source model and
repair outcome are known.

Uniform random scan, deterministic sweep, reversibility and quantitative mixing
are different claims. Do not infer reversibility of a composition from common
invariance, or aperiodicity/mixing from either invariance or conditional-law
existence. No executable conditional-sampling cost has been formalized here.

## Integration guard

Registry remains 394. Only kernel powers owns the single stabilization lane;
the locally verified queue is not permission to fabricate MERGED or to promote
every declaration to Registry. Remote push/PR update/merge/deployment remains
unapproved. All bounded work stays in the original D: repository and this chat.
