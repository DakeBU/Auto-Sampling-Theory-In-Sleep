# Next route after the actual random-scan transition law

This is a scoped candidate handoff, not a frozen theorem or a new Goal. Resolve
the canonical independent proof/source gates for the current packet first.

## Reuse, do not rebuild

`RandomScanHeatBath.randomScan` is the actual uniform finite mixture of the
canonical coordinate kernels. The compiled `randomScan_apply_singleton`
identifies its probabilities at every source-supported start, including the
exact one-update factor `1/(n+1)`. Conditional versions at null fibers remain
outside that pointwise law. Existing invariance and invariant powers have real
consumer tests; another alias for these facts is not a new contribution.

## Smallest useful candidate

Audit the next source-backed **detailed-balance edge for this same kernel**.
Search existing ASTIS, pinned Mathlib, external reuse records and the precise
source anchors before freezing anything. A candidate finite flux identity is

\[
 \mu(\{x\})P(x,\{y\})=\mu(\{y\})P(y,\{x\}).
\]

It should cover arbitrary ambient `x,y`, allowing zero target atoms, without
pretending the positive-start transition formula applies at such null starts.
For two positive atoms, matched retained coordinates give identical fibers and
the two flux numerators agree; for a zero atom use the opposite supported-start
law, or zero factors when both vanish. All inverse cancellations must justify
positive, finite masses.

Prefer a packet that consumes this flux in Mathlib's existing
`Kernel.IsReversible`, rather than a second definition of reversibility or an
unconsumed scalar wrapper. Do not make a supported-state subtype kernel a
prerequisite merely for naming the source domain: the present ambient-kernel
contract already excludes invalid starts where needed. If a later consumer
really needs that restriction, it remains a distinct mathematical edge.

Read-only API preflight at Mathlib commit
`db584cd6d46c92f209a44c0f1c829460d327499d` found:

- `Probability/Kernel/Invariance.lean`: `Kernel.IsReversible` is equality of
  two set lintegrals, with `IsReversible.invariant` already proved. Do not
  re-formalize either existing fact.
- `Probability/Kernel/Composition/MeasureCompProd.lean`:
  `Measure.compProd_apply_prod` relates rectangle mass and set lintegrals.
- `MeasureTheory/Measure/Dirac.lean`: `Measure.ext_of_singleton` for countable
  measurable-singleton spaces.
- `MeasureTheory/Integral/Lebesgue/Countable.lean` provides discrete integration
  formulas. Inspect exact signatures before choosing the shortest bridge.

This preflight is not a typed/API certificate, a new public lemma or a completed
SAU. The one Worker must choose the minimal actual consumer and retire a tactic
route after repeated unchanged failures.

## Preserve source regimes and strict boundaries

Discrete Sampling section 1.3, PDF p.5, states detailed balance in its **hard-core
model discussion**, then gives the general conditional update. A general-target
balance theorem may be a reusable standard derivation, but must not be called
an exact numbered-source statement without the precise source/assumption audit.
In particular, that page's hard-core irreducibility and aperiodicity argument
must not be transferred to arbitrary supported Boolean targets: the compiled
diagonal-target example is disconnected under single-site moves.

No mixing, spectral gap, cost, concrete Gibbs normalization, general-state MH,
or continuous-time statement follows just from detailed balance. The separate
source-copy index overlay never licenses silent rewriting of the source PDF.
Any new source-facing proposition requires a fresh blind reconstruction and
independent source comparison; reuse the existing compact evidence, not prior
reviewer conclusions as decoder input.

Reviewer-packet reminder from this run: every delta needs both `description`
and supporting `evidence` (an `impact` field alone is insufficient). Evidence
must support that particular delta; a shared slot label alone does not justify
copying unrelated slot evidence. Keep public evidence paths repository-relative
and pin immutable original responses and any same-reviewer schema amendments.

## Integration and storage

Keep the original D: repository and physical `.lake` cache. Registry remains
394; kernel powers retains the only `STABILIZING` lane. Independently verified
queued packets are not merged or Registry-integrated. Remote push, PR mutation,
merge and deployment still need explicit human authority. No replacement Goal,
parallel checkout, detached harness or permanent background Worker is needed.
