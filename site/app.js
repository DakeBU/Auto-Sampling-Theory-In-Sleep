const tabs=document.querySelectorAll('[data-tab]');tabs.forEach(b=>b.addEventListener('click',()=>{tabs.forEach(x=>x.classList.remove('active'));document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.querySelector('#tab-'+b.dataset.tab).classList.add('active')}));const families={metric:{title:'Metric gradient flow',formula:'E˙ = −‖grad_g E‖²; PL-shaped coercivity closes the scalar differential inequality.',body:'Euclidean, Riemannian and Wasserstein gradient-flow arguments share a dissipation skeleton, but the state space, metric and gradient must remain typed separately.'},curvature:{title:'Curvature → quadratic growth',formula:'lower curvature → first-order quadratic model → growth / coercivity in the setting where the implication is valid',body:'Euclidean strong convexity, geodesic convexity, displacement convexity and Bakry–Émery curvature are related patterns, not interchangeable assumptions.'},gap:{title:'Gap → gradient / information dissipation',formula:'PL: ‖∇f‖² ≥ 2α(f−f*)  |  LSI mirror: Fisher ≥ 2α KL',body:'PL and LSI are connected at the mechanism level through energy dissipation; the hyperedge is conceptual unless an exact transport theorem is separately formalized.'},l2:{title:'L² coercivity',formula:'Poincaré + Dirichlet dissipation → χ² exponential decay',body:'This branch mirrors the gap–dissipation–decay pattern but remains distinct from the KL/Fisher/LSI branch.'},prox:{title:'Proximal energy',formula:'one quadratic-regularized energy → deterministic proximal minimizer or Gibbs/RGO draw',body:'The shared object is the regularized energy; deterministic and randomized consumers have different semantics and certificates.'}};const detail=document.querySelector('#family-detail');function show(k){const f=families[k];detail.innerHTML='<h3>'+f.title+'</h3><p><b>'+f.formula+'</b></p><p>'+f.body+'</p>'}document.querySelectorAll('[data-family]').forEach(b=>b.addEventListener('click',()=>show(b.dataset.family)));show('metric');