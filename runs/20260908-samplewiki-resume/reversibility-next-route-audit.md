# Return to the SampleWiki analytic spine after the kernel integration

This is a scheduling audit, not a frozen theorem, new Goal, typed blocker, or
additional substantive advance. Finish the current independent proof and source
gates before assigning the next single-writer packet. The sole stabilization
lane and publication authority remain unchanged.

## Reuse now available

The actual uniform Boolean random-scan kernel has its supported transition law
and a local proof of Mathlib reversibility. The reusable countable atomic bridge
has a real consumer in that proof. Neither fact supplies quantitative mixing.
Do not next add another alias for invariance or finite powers: both interfaces
already have consumer tests. A concrete Gibbs model, its supported-state kernel,
connectivity, spectral inequalities and computational cost remain separate work.

The eight-member `kernel-cell-synthesis-v2.json` is a historical compact reuse
snapshot at `bb6fa81`; the reversibility worker packet and review are its later
addition, not a reason to rewrite the snapshot's original proof provenance.

## Why the next audit returns to SampleWiki

`proof-blueprints/samplewiki-frontier-formalization-plan.md` identifies the
Chapter 1 Fisher/transport and flow interfaces as prerequisites of Chewi 8.4.1
and the ideal-proximal frontier. Dependency order, not accumulating more small
finite-state facts, should choose the next packet. Inspect the latest canonical
metadata and primary source before freezing a statement; old execution lists
are not proof evidence and can lag the present source tree.

Read-only inspection in this run found the following **existing** interfaces:

- `InformationTheory/CanonicalRelativeFisher.lean`: the smooth finite score
  domain already exposes absolute continuity, a.e. differentiability and squared
  score integrability. It is not the full extended-valued Sobolev domain.
- `InformationTheory/CanonicalKLDissipation.lean`: `FlowDerivativeDomain`
  contains the actual KL derivative-under-the-integral formula as an explicit
  field, together with finite entropy near the time, source integrability,
  mass conservation and the forward equation. This is not itself a proof of
  that differentiation theorem.
- `InformationTheory/GeodesicFisherTransport.lean`: the geodesic first-order
  and scalar square-root closure already exist. Its analytic inputs remain
  `HasDerivAt` for the selected path and a score/transport pairing bound. A
  second real-algebra wrapper would not close the source-level edge.

The current `website/content/chapter_1_completion_matrix.json` still marks
Theorem 1.4.5 as planned, at printed p.34 / PDF p.46, with no required
declarations or focused tests attached. The edition metadata pins the
2026-08-09 PDF. These are retrieval leads to verify against the actual PDF,
not permission to infer exact source assumptions from the generic section
template. In particular, audit endpoint one-sided versus two-sided derivative
requirements before supplying the existing `HasDerivAt` interface.

These observations are API provenance, not a fresh compilation or source
round-trip for these older files. The next bounded audit should inspect their
actual consumers, existing transport/entropy modules, precise source equations
and Mathlib reuse. Select only one genuinely missing analytic edge, or a
reviewer-confirmed typed blocker that identifies the unsatisfied hypothesis.
Do not infer the full measure-level Fisher--Wasserstein result by supplying its
essential first variation as an unexplained assumption.

## Persistent boundaries and efficient execution

Keep the source-field integrability, derivative/limit interchange, whole-space
weighted IBP, generator/semigroup domains and invariant-law branches distinct.
No second-order cutoff branch is justified without a real second-order consumer.
General-state MH support/RN arithmetic, finite-jump mLSI zero-density domains,
and SDE construction are not silently absorbed into this scheduling audit.

Use one bounded Worker, focused compilation, exact evidence capsules, and one
independent verifier; add fresh blind/source reviewers only for source-facing
statements. Preserve exact snapshots, avoid replaying entire transcripts, and
keep owned files/cache in the original repository. No remote push, merge or
deployment is authorized by an automatic Goal continuation.
