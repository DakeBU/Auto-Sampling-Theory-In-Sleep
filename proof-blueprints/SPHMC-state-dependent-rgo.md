# State-dependent RGO posterior update and recovery

For a measurable state space S, fixed probability base mu on finite real Hilbert Borel E, measurable b:S->R,u:S->E,a:S->R with b>=0 and a>0 everywhere, construct Markov kernels T,H:S->E and B:S times E->S times E. Prove every T(s) is the normalized quadratic mu tilt at precision b(s), center u(s); H(s) is its actual Gaussian smoothing with variance a(s); B(s,y) retains s and samples the actual backward tilt, equivalently the original mu tilt with precision b(s)+1/a(s) and center (b(s)u(s)+y/a(s))/(b(s)+1/a(s)). For every probability state law lambda prove B composed with lambda compProd H equals lambda compProd T. Derive jointly measurable positive finite normalization and variable noise kernels; do not assume measurable target selection or desired recovery.

One actual state-dependent ideal RGO stage, not complete Algorithm3.3. The state is specified before the new Gaussian draw and may encode current history. The theorem does not condition retrospectively on a reported smoothing level while assuming an unchanged sample law. Actual joint sampler/report semantics, approximate guarantees, random stopping, FORS, recursive error budgets and expected query costs remain open. No deterministic variance schedule is imposed or inferred from the source.

Construct normalized weight exp(-b(s)*norm(x-u(s))^2/2) directly: jointly measurable, positive integral and bounded by one. Build updated target from the same parameter theorem. Construct noise by mapping independent standard Gaussian with sqrt(a(s)); prove fibers. Use existing pointwise actual recovery to establish the globally constructed posterior sections, then Tonelli on measurable event sections proves retained-state joint recovery. Existence at each fixed parameter alone is insufficient for joint measurability.

Independent source review rejected the earlier unregistered deterministic-schedule plan as insufficient for complete Algorithm3.3. No such restricted theorem was developed or admitted. Source report/output coupling remains an explicit later obligation.

Source-interface clarification after independent recheck of Sections6.1 and7.1: eta is fixed for each call input, selected measurably as eta(s); s may itself be random. A random runtime report/output joint contract is an optional extension, not an additional paper obligation. The earlier report-coupling caution must not be read as a source defect. Actual approximate output kernels, state measurability, recursion, termination and costs remain open.
