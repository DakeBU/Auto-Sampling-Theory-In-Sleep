# Actual reference-carrying query cost

SPHMC arXiv:2609.06906v1 Algorithm3.3 and Section6.3 actual reference-point gradient query cost under the recursive state law

For the actual reference-carrying state and first inner gradient-square hit N,define C=0 at absorbed states and C=N+1 otherwise. Prove joint measurability. From actual original-coordinate Markov output M(s) with source normalized-call W2 budget kappa_s*eps_s^2/beta_s,derive per-state integrability and explicit expectation bound 2+kappa_plus*log(1+4*(eta+tau)*(1+eta)+2*(eta+tau)*kappa_s*eps_s^2/d+tau/(eta+tau)),zero in the absorbed branch. Integrate the actual nonnegative cost under lambda compProd (M product standardGaussian); derive global L1 and real expected-cost bound when the explicit upper bound is lambda-integrable. Keep the actual first-hit,pre-noise initial point and source-completed-square trajectory correspondence.

Supplied current-call W2 accuracy is the source A1 input,not a proved sampler implementation. Actual moments,first-hit count,per-state integrability and kernel-law expectation must be derived. No TV transfer of unbounded costs,no unconditional global L1,no source stopping-depth or precision schedule,history invariants,stage sum,terminal FORS or complete paper result. N+1 counts visited gradient checks with source explicit reuse convention; absorbed states execute no reference GD.

# Next dependency-ready source edge: actual statewise reference cost

Planning only, independently source-reviewed by log_depth_source_review. Current ReferenceCarryingKernel is sole stabilization owner. No new SAU or Lean edits yet.

Source: SPHMC arXiv:2609.06906v1 Algorithm3.3 and Section6.3. Reuse exactly the actual first-hit N and state/update of ReferenceCarryingKernel; do not substitute the stricter terminal index. Define actual charged count C(s,x,z)=0 when theta<=b, else N(s,x,z)+1. Absorbed states do not run GD or charge a gradient check.

Let beta_s=beta+b,alpha_s=alpha+b,kappa_s=beta_s/alpha_s,v=(eta+tau)/beta_s,bplus=b+1/v,kappa_plus=(beta+bplus)/(alpha+bplus). Current potential V_s=V+b/2*norm(.-u)^2. From actual Markov output M(s) with source A1 input W2^2(M(s),H_(eta/beta_s) pi_(V_s))<=kappa_s*eps_s^2/beta_s, derive (not assume) gradient-square moment <=beta_s*(4*d+4*eta*d+2*kappa_s*eps_s^2). The new independent Gaussian contributes tau*d/(beta_s*v^2) to the true initial regularized-gradient square. No TV cost transfer.

Target: C jointly measurable; per active state C integrable under actual M(s) product standardGaussian, expected C <=2+kappa_plus*log(1+4*(eta+tau)*(1+eta)+2*(eta+tau)*kappa_s*eps_s^2/d+tau/(eta+tau)). Absorbed state expectation=0. All parameters/domain conditions and the supplied W2 A1 guarantee explicit. This is actual cost derivation, not construction of accuracy-achieving M.

Then integrate over arbitrary actual initial state law lambda and real joint kernel law lambda compProd (M product G), first as nonnegative extended integrals. Do not claim unconditional global L1: derive it only when the displayed statewise upper bound is integrable under lambda. Missing edges: same actual N count comparison, varying regularized potential/parameters in actual moment theorem, measurable kernel integration. No stage sum, source tau schedule, precision allocation, depth or history-integrability closure yet.

Reuse TerminalReferenceGradientDescent pointwise count only (never fixed-center expectation), ApproximateInitialGradientMoment at f=V_s, smoothing eta/beta_s and new noise tau/beta_s; JointReferenceGradientDescent log-integrability/tangent method; ReferenceCarryingKernel actual first-hit definition and quadratic/source correspondence; Measure.lintegral_compProd and kernel integral measurability. Need make private actual-index formulas definitionally align, not expose/change existing compiled theorem contracts.

Alternative larger terminal FORS route deferred: PoissonRejection.poisson_rejection_output already constructs a parameter kernel for fixed auxiliary law and B, jointly measurable estimator. State-varying terminal law uses varying Gaussian auxiliary; fixed uniform times standard Gaussian reparameterization can move sqrt(eta) into estimator, but actual first-success/attempt/count equivalence and joint state/random-stream measurability must be proved. Pointwise Measurable output alone does not imply a parameter kernel.

Implementation reconnaissance (read-only, pinned current Mathlib): JointReferenceGradientDescent public theorem already returns actual first-hit minimality, integrability and count bound for any genuine C2 potential. Apply to Vs with alpha+b,beta+b and eta/beta_s,tau/beta_s,r=sqrt(kappa_s*eps_s^2/beta_s). Use current source gradient and trajectory identities plus uniqueness of least hit to identify the actual N; no private parent name or duplicate count proof required. The expanded argument is exactly v*(M+tau_actual*d/v^2)/d. Its simplification is 4*(eta+tau)*(1+eta)+2*(eta+tau)*kappa_s*eps_s^2/d+tau/(eta+tau).

Mathlib Probability/Kernel/Composition/IntegralCompProd.lean:461 Measure.integrable_compProd_iff requires joint AEStronglyMeasurable, per-state AE integrability and integrability of conditional norm integral. :469 Measure.integral_compProd gives actual iterated real integral after L1. Need conditional norm nonnegative and upper bound to derive global L1. NormalizedReferenceCall.private regularized_hessian is exact short genuine derivative proof pattern; existing public QuadraticRegularization only exports strong convexity and gradient Lipschitz, not shifted Hessian, so cannot pretend its hidden Hessian proof is public. Keep necessary derivative calculation private under substantive cost consumer if no exact exported parent found.
