# Next actual terminal-sampler edge (planning, not a proved result)

Consumer: SPHMC arXiv:2609.06906v1 Theorem A.4(2), via smooth s=1 case of arXiv:2602.01338v1 D.1. Current parents are ClippedGradientProgram (actual iid program/output/cost) and ClippedRenyiComparison (actual ideal versus clipped laws, both RN power moments). This plan does not close A.4(2).

Candidate explicit parameter route: fix B=1; order ell>=2; 0<eps<=1/2; L=ell+log(1/eps). Use sufficient inverse-step condition eta^-1>=64*beta*(sqrt(d*L)+L), retaining positive dimension/beta/eta and the supplied reference residual. Derive the existing 64-step condition from this inequality. Derive K=min(1/(40 beta^2 d eta^2),1/(8 beta eta))>=8L: first branch >=(4096/40)L, second >=8L. Positivity is needed before squaring/dividing. Since L>=2+log(1/eps), 2*exp(2-K)<=eps^2 follows with ample slack. These are mathematical derivations to formalize, not accepted Lean claims.

Then prove the actual output law from the parent's measurable first-success output has both real RN power integrals <=1+eps^2. Establish positivity of each moment (mutual AC, probability, nonzero density) before applying logarithms. The explicit logarithmic expression log(integral RN^ell)/(ell-1)<=eps^2 follows from log(1+t)<=t and ell-1>=1. If a reusable canonical Renyi definition exists, identify it; otherwise state the actual expression clearly and disclose API boundary, avoiding a fake named divergence.

Return the actual parent program and its output pushforward in the new source-backed consumer, not merely an unrelated pair of abstract measures. With B=1, the parent establishes expected estimator calls <=2*exp(2). Gradient accounting must include the cached gradient at xp (one call) and each W evaluation's one new gradient evaluation, reusing the cached reference gradient; the exact cached count is 1+estimator count, with expectation at most 1+2*exp(2). Bind this count to the explicit calling convention, rather than label estimator calls as gradient queries. No TV cost transport. The full-batch implementation includes successful batch; no early-rejection saving assumed.

A.4(2)'s logarithmic high-probability gradient-query tail is still an additional required theorem. Its proof must handle dependence between acceptance and batch size; an independent geometric count plus independent Poisson total cannot be assumed. A valid route can bound the event by failure to stop in n retries plus the Poisson sum of the first n batch counts, which are iid unconditionally. Need explicit measurable count/tail and concentration proof. Joint input measurability, supplied-reference initialization and the outer SPHMC composition remain separate boundaries. Convexity is required by the SPHMC application but not D.1's fixed smooth comparison.

Independent route review log_depth_source_review accepted the numerical implication and logarithmic route. Existing PoissonQueryTail.poisson_query_tail supplies the actual full-batch tail; instantiate its stream/event/count definitions instead of reproving Poisson concentration. At B=1 its K1=2*(exp(1)-1)*(exp(2)+1/log(2))+1 gives estimator tail K1*log(2/p); cached gradient count uses K1+1/log(2). Source Appendix C final parameter substitution does not directly imply its claimed second delta/2 term; consume the independently proved local tail and preserve this disclosure. No new Lean proof or theorem admission from this route review.

Implementation reuse verified in the current checkout:
- `ClippedGradientProgram.clipped_gradient_program` returns the measurable actual first-success output, its pushforward, actual iid attempt law, acceptance probability, and exact/full-batch expected estimator cost. Its helper definitions are private: the consumer must use definitionally identical concrete definitions, not reference inaccessible private names or assume output-law equality.
- `PoissonQueryTail.poisson_query_tail` takes a Markov kernel Q:S->X and the jointly measurable bounded estimator. For a fixed call instantiate a constant kernel on a singleton input space. Its attempt stream uses X x (Nat x ((Nat->A) x Real)), as does the clipped gradient program; prove definitional equality of law, acceptance event and count. This fixed-input instantiation does not establish joint measurability in x0/xp/eta.
- `TechnicalLemmas/InformationTheory/Renyi.lean` explicitly defines density algebra only. The logarithmic RN moment expression must therefore be stated explicitly, with actual probabilities, absolute continuity, positivity and integrability, unless a canonical divergence definition is independently introduced and justified.
- Mathlib `Real.log_le_sub_one_of_pos` is available for positive actual moments; no use of totalized log at zero as a surrogate divergence proof. True RN moment positivity can be obtained from probability/absolute continuity and density nonzero, or a Jensen lower bound after establishing density L1.
- Exact positivity adapter: `Measure.rnDeriv_pos'` applied to denominator absolutely continuous with respect to numerator gives numerator/denominator RN positivity denominator-a.e.; pair it with `Measure.rnDeriv_ne_top` under that same denominator before `ENNReal.toReal_pos` and `Real.rpow_pos_of_pos`. Use `integral_pos_iff_support_of_nonneg_ae` with the actual L1 and probability mass of the a.e.-full support. Do not confuse `rnDeriv_pos` (numerator-a.e.) with the primed lemma (the needed denominator-a.e. statement under reverse AC).
