# Actual enhanced-state terminal execution

SPHMC arXiv:2609.06906v1 Algorithm3.3, Section6.3 (6.4) and terminal stage

Prove actual reference-carrying recursive kernel reaches its fixed terminal domain by the explicit logarithmic depth. Use source epsilon_J=Delta0/sqrt(J+1), prove the terminal64 compatibility from local sufficient constants, and connect the actual TerminalFORSKernel at this precision. Return finite output kernels and cap stability on the full retained state, without projecting away the reference.

Normalized beta=1, positive dimension and genuine C2 Hessian bounds with alpha=1/kappa. Fixed initial state and recursion accuracy Delta0. Local sufficient C=8,gamma=1/1024 are not quoted source constants. Supplied actual M remains arbitrary Markov; no recursive output accuracy, M construction, initialization-cost integration, accumulated query bound or full main theorem. Almost-sure finite depth does not establish budget integrability. No TV transfer of unbounded cost.

# Next route: actual enhanced-state termination and terminal output

Planning only, selected by independent log_depth_source_review after TerminalFORSKernel. Current terminal packet remains sole stabilization owner. No new SAU or Lean edits yet.

Source consumer: SPHMC Algorithm3.3/Section6.3 actual finite recursive execution. Use normalized beta=1,alpha=1/kappa and retained state(b,u,xref,k,history). Confirm exact kappa/domain hypotheses against LogarithmicDepth before proposal. Measurable eta(s) obeys0<eta<=c<1/4. K(b)=(1+b)/(1/kappa+b); tau=K whenK>=2 and c otherwise; v=(eta+tau)/(1+b). M is an actual original-coordinate Markov kernel; no accuracy premise in this packet.

Use the real ReferenceCarryingKernel update: independent Gaussian disturbance, precision/center update, actual first GD hit from the pre-noise output, reference retention and history. Prove precision progress along full retained-state paths; eta and M may depend on xref. Do not identify the reference-deleted process with an autonomous Markov kernel. Reuse methods from StoppedRGODepth, not its state type by renaming.

Choose positive terminal threshold Theta meeting current TerminalFORSKernel explicit64 condition, not merely an older B^-1 bound. Prove for a derived finite depth J that(P^J)(s0){b>=Theta}=1 by path-depth and kernel-support induction. Build actual terminal output L using the terminal subtype, or use btilde=max(b,Theta) on the whole state and prove it equals the genuine parameters/program on the terminal set. Domain-outside extension is residual cap behavior only. Prove actual finite output R_n=L compose P^n and R_(J+m)(s0)=R_J(s0), eliminating dependence on that extension after J.

Actual public parents: ReferenceCarryingKernel.reference_carrying_kernel; TerminalFORSKernel.terminal_fors_kernel; LogarithmicDepth.terminal_depth. FiniteRGOProgram and StoppedRGODepth give finite recursion/kernel-power/absorption proof methods but require real enhanced-state adaptation.

Boundary: complete finite terminating program relative to supplied actual M, not a main sampling-accuracy result. M construction/accuracy, source error propagation, terminal GD initialization-cost integration, state budget integrability and stage-query totals remain. Almost-sure finite depth does not imply Integrable B or justify TV transfer of unbounded cost. Next costs follow this execution-semantic connection, not another desired-cost wrapper.

Pinned public depth signature rechecked by root: LogarithmicDepth.terminal_depth requires kappa>=1,0<c<1/4,r0>=0,all0<eta_n<=c,positive Nat d,q>=2,0<Delta<=1/2,0<gamma<=1,C>=8. Its L=q+log(K(r0)*d*q/Delta), B=gamma/(sqrt(d*L)+L), J=ceil(C*log(exp(1)*K(r0)/B)); it proves r_J^-1<=B and an explicit upper-depth coefficient. Thus terminal precision compatibility is a genuine extra adapter: source terminal eps must be pinned and the64 condition proved from the chosen gamma/L, not assumed from oldB alone. Do not guess eps=Delta. The natural expression Delta/(K(r0)*d*q) makes log(1/eps) match L-q, but this is only algebraic reconnaissance until primary source call parameters are checked.

Primary-source planning review, log_depth_source_review, 2026-09-12: Section6.3 (6.4) and terminal stage use terminal epsilon_J=Delta0/sqrt(J+1), not Delta0 or Delta0/(K0*d*q). Delta0 belongs to the current Theorem6.5 recursive call, not automatically Theorem6.1 outer accuracy. A1 ell=q=2; A2/A3 ell=q>=2. Suggested local sufficient choices C=8,gamma=1/1024 give J<=64L, log(J+1)<=4L, H=q+log(1/epsilon_J)<=3L and64(sqrt(dH)+H)<=192(sqrt(dL)+L)<=B^-1. These inequalities remain to be proved in Lean; no source numerical-constant claim. Retain arbitrary actual M; execution/terminal accuracy only, no whole-recursion accuracy or accumulated cost.
