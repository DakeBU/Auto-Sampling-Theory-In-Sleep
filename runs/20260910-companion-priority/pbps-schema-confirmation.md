The independent reviewer `gaussian_independent_verifier` confirmed in the
current task, after returning the mathematical/source review:

> Confirmed. Record all four deltas as `severity: "informational"`, mapping
> `delta` to `description` and `disposition` to `evidence`. The accepted,
> generative-reflection-only scope remains unchanged.

This is schema normalization only. The original selected review evidence is
preserved in `pbps.source-review-result.json`; its evidence hash is unchanged.
