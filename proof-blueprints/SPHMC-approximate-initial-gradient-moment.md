# Actual approximate initial gradient moment

SPHMC arXiv:2609.06906v1 Section6.3 Reference point construction: actual approximate and regularized initial gradient moments

For genuine C2 U with alpha/beta Hessian bounds,0<alpha<=beta, define actual pi=volume.tilted(-U) and rho_eta=gaussianSmoothing(pi,sqrt eta). For eta>=0,tau>0,r>=0 and actual probability nu with W2(nu,rho_eta)^2<=ofReal(r^2), prove actual gradient-square L1(nu) and expectation<=M=4*beta*d+4*beta^2*eta*d+2*beta^2*r^2. On actual nu.prod(stdGaussian), define F_yz(x)=U(x)+norm(x-(y+sqrt(tau)*z))^2/(2*(eta+tau)). Prove grad F_yz(y)=grad U(y)-sqrt(tau)/(eta+tau)*z, its joint square L1, and exact expected square = integral norm(grad U)^2 dnu + tau*d/(eta+tau)^2, hence <=M+tau*d/(eta+tau)^2. Derive ideal/smoothed/actual moments, all coupling/product integrability and Gaussian cross-term cancellation rather than assuming them.

Fixed-call actual input moment only, including eta0/r0/dim0. Coordinate-free/general-beta formulation disclosed. No optimal coupling, marginal state-second-moment, gradient-moment or cross-term-integrability input is assumed. W2 is the actual extended-real coupling-cost definition. Gaussian independence is the explicit product law. Random-history conditional moment, jointly measurable GD stopping with the random center, summed reference cost and complete sampler remain separate. Source specialization beta1,r^2=kappa_U*eps_j^2,eta<=c0<1/4 permits derived sufficient constant5, not a printed source numerical constant.

See the independently reviewed route in .astis/approximate-initial-gradient-moment-route.json. Prove Gaussian second moment by basis variance and Parseval. Integrate the actual smoothing product, then transfer via near-optimal couplings. Do not require an optimal coupling at r=0. Before eliminating the independent Gaussian cross term, prove absolute integrability. The random center uses the same pair as the initial point, so fixed-center expected GD bounds cannot be silently applied.
