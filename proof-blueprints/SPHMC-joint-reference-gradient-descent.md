# Actual joint inner reference gradient descent

SPHMC arXiv:2609.06906v1 Section6.3 Reference point construction: actual inner gradient descent at the shared random center

For genuine C2 U with 0<alpha<=beta Hessian bounds, eta>=0,tau>0,r>=0 and positive dimension d, let A=eta+tau,a=alpha+1/A,c=beta+1/A,k=c/a. Under actual probability nu satisfying W2(nu,rho_eta)^2<=ofReal(r^2), use the same pair p=(y,z) for x0=y and center u=y+sqrt(tau)*z. Define actual GD with step1/c and first hitting of norm(grad F_p)^2<=a*d. Prove finite stopping, joint measurability of stopping index and output, first-hit property, residual identity and bound A*sqrt(a*d), pointwise N+1<=2+k*log(1+A*norm(grad F_p(y))^2/d), actual pair-law count integrability and expected N+1<=2+k*log(1+A*(M+tau*d/A^2)/d), where M=4*beta*d+4*beta^2*eta*d+2*beta^2*r^2.

Actual inner threshold a*d is preserved. Terminal d/A stopping is used only as an existence witness and larger count, not substituted for the inner algorithm or equated with its index. The resulting logarithmic bound is a disclosed sufficient upper bound. Count N+1 includes initial/final gradient checks. No evaluator caching, source upstream Wasserstein guarantee, conditional random history, stage sum or full sampler/main-result closure. Positive dimension matches the source and terminal parent. General beta and explicit moment constants are disclosed adaptations.

# Next dependency route: same-pair reference GD

Draft planning only. No next SAU or Lean declaration is recorded here. Independent log_depth_source_review accepted the corrected inner-rule route and its sufficient fixed-call cost estimate; this is route review, not proof or source admission.

Source review correction: Section6.3 inner reference construction stops at a*d, whereas the existing terminal theorem stops at d/A. Do not replace the actual inner algorithm with the stricter terminal stopping rule merely to reuse a proof. Define first hitting of a*d and use the existing d/A stopping time only as a witness/upper bound, since d/A<=a*d. Thus N_actual<=N_strict, without claiming equality. The resulting log(1+A*moment/d) count estimate is a separately disclosed looser sufficient bound. For source beta=1, A=eta+tau<=c0+max(kappa_U,c0), its logarithmic argument is controlled by 4*A*(1+eta)+2*A*r^2/d+1, compatible with the cited per-call polylog estimate when the source r^2=kappa_U*epsilon_j^2 guarantee is available. That guarantee and the all-stage sum remain unproved here. Terminal d/A and inner a*d remain distinct actual branches.

The actual input moment theorem supplies the joint L1 and bound for grad(F_yz)(y) under nu.prod(stdGaussian). Its consumer must preserve that same pair in both the initial point y and the center u=y+sqrt(tau)*z. Applying the old fixed-center expected-cost clause to an unconditional marginal would not do this.

Use A=eta+tau, a=alpha+1/A, c=beta+1/A and k=c/a, with positive d. The genuine gradient formula gives the joint map T(u,x)=x-c^-1*(grad U(x)+A^-1*(x-u)). Its finite iterates are jointly continuous in u,x. On p=(y,z), define x_n(p)=T(u(p),.)^n(y), then the actual inner predicate norm(grad F_p(x_n(p)))^2<=a*d is measurable in p for every n.

The existing public TerminalReferenceGradientDescent theorem already proves finite stopping and pointwise count bounds for every fixed center and initial point. Its strict stopping index supplies an existence witness for the inner predicate; Nat.find minimality proves the inequality to that strict index. Do not import its private firstIndex helper, identify the two stopping times, or duplicate analytic contraction. The inner residual estimate is A*sqrt(a*d), not the terminal sqrt(d*A).

Measurable.find then supplies the actual joint stopping count and stopped output. The existing pointwise inequality gives N(p)+1<=2+k*log(1+A*norm(grad F_p(y))^2/d), with the final gradient check included. Integrate under the actual pair law using the proved initial joint moment and a legitimate log tangent/Jensen argument after L1, yielding the expected bound with M+tau*d/A^2.

This advances a fixed-call joint stopped program. Conditional random histories, the law/precision guaranteed by the actual upstream sampler, allocation across recursive stages and full summed query cost remain separate. Dimension-zero behavior is not to be silently inherited from the current moment theorem: the existing GD threshold/count theorem uses d>0, matching the source domain.

Implementation route, not compiled evidence: represent the inner first index by `if h : exists n, stop p n then Nat.find h else 0`, and prove existence from the terminal public pointwise witness. Prove joint iterate continuity by induction on n using the actual gradient formula and continuous gradient U. This avoids needing a parameter-dependent iterate API. `measurable_find` and `Measurable.find` then establish the index/output measurability from the measurable predicates, as in the existing stopped-program proof. The false branch is proved unreachable, not assumed away. The private terminal index may occur through its public theorem's type, but is never referenced by its private declaration name.

Reuse the existing expected-count proof pattern as a private implementation detail over X=E*E, with actual g(p)=norm(grad F_p(p.1))^2, s=d/A and actual pair law. Its tangent inequality proves log integrability before integrating; then count domination yields count L1. This is a new actual joint-program consumer of the moment theorem, not a restatement of an assumed cost moment. The count is N+1, including the gradient check at n=0 and the final successful check; evaluator caching and composition with another sampler need separate trace/cost arguments.
